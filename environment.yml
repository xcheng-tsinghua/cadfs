name: cadfs
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pythonocc-core=7.9.0
  - open3d=0.19.0
  - pip
  - pip:
    - -e .
    # For inference (vllm + qwen-vl-utils):
    #   pip install torch==2.8.0 --extra-index-url https://download.pytorch.org/whl/cu126
    #   pip install -e ".[inference]"
